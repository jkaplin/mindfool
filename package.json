{
  "name": "mindfool",
  "version": "1.0.0",
  "homepage": "https://jkaplin.github.io/mindfool/",
  "main": "src/index.html",
  "scripts": {
    "predeploy": "rm -rf dist && parcel build src/index.html --public-url /mindfool",
    "deploy": "gh-pages -d dist",
    "build": "parcel build src/index.html --public-url ./",
    "watch": "parcel watch src/index.html",
    "start": "parcel serve src/index.html",
    "clean": "rm -v dist/*"
  },
  "license": "ISC",
  "dependencies": {
    "phaser": "^3.20.1"
  },
  "devDependencies": {
    "gh-pages": "^2.2.0",
    "parcel-bundler": "^1.12.4"
  }
}
